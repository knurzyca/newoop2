package wsb.devices;

public class App   {
}
/*final Double price;

public App(String name, String version, Double price) {
        super(name, version);
        this.price = price;
    }

/*
Create new class App with name, version and price of an app
        Add a set of apps to the Phone
        Add installAnApp method where
        You check if owner has enough money
        Add an app into the set
        Pay for it (decrease cash amount)
        Add a method that check if you have an app already installed by app object
        Add a method that check if you have an app already installed by name
        Add a method that will list all installed apps to console
        Add a method that will list all installed apps to console sorted by price
        Add a method that will list all installed apps to console sorted by name
*/